import styled from 'styled-components';
import { colors } from '../../../utils/colors';

const H1 = styled.h1`
    font-size: 5rem;
    color: ${colors.white};
    text-align: center;
    font-weight: 200;
`

export default H1;