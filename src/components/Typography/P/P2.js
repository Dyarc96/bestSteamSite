import styled from 'styled-components';
import { colors } from '../../../utils/colors';

const P2 = styled.p`
    font-size: 4rem;
    color: ${colors.black};
    margin: 0 auto;
    padding-top: 2rem;
    text-align: center;
    width: 90%;
    line-height: 1.2;
`

export default P2;